import SwiftUI

struct RoutingView: View {
    var body: some View {
        VStack(alignment: .leading, spacing: 12) {
            Text("Routing").font(.title)
            Text("BlackHole/Multi-Output Guide + Checks")
                .foregroundStyle(.secondary)
            Spacer()
        }
        .padding(16)
        .frame(minWidth: 800, minHeight: 600)
    }
}

