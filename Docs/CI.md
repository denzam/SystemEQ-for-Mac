# CI Plan (Initial)

- GitHub Actions macOS runners (macos-13 or later), Xcode 15+
- Jobs: build Universal (arm64+x86_64), run unit tests (when added), archive artifact.
- Signing/Notarization: add in Phase 3.
