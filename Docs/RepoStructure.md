# Repo Structure & Naming (Planned)

- `SystemEQ for Mac/` — SwiftUI app (current Xcode target)
  - `Features/` (later): Equalizer, Calibration, Headphones, Routing, Presets, Settings
  - `AudioEngine/` (later): AVAudioEngine wrappers, device routing
  - `DSP/` (later): Parametric EQ, Convolution
  - `UI/` (later): Components (Meters, Sliders, Toggles)
  - `Data/` (later): Presets, AutoEQ imports
- `HALPlugin/` (Phase 3)
- `Docs/` — planning, specs, wireframes, tokens
